# Local PostgreSQL connection (for development on your machine)
# Use the SAME username and password you use in pgAdmin to connect to PostgreSQL.
# If your password contains @ # % : / or other special chars, URL-encode them (e.g. @ = %40).
# Port: 5432 is default; use 5433 if your PostgreSQL runs on that port.
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5433/finances_app?schema=public"

# --- Vercel / production ---
# Keep using your local PostgreSQL for development; do NOT commit .env to GitHub.
# On Vercel: leave DATABASE_URL unset if you only want the app to work locally (the deployed
# site will show a database connection message). When you want the live site to use a
# database, set DATABASE_URL in Vercel Project Settings → Environment Variables to a
# hosted PostgreSQL URL (e.g. Neon, Vercel Postgres, Supabase).
